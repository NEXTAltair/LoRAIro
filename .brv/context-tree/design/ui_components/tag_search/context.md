
The tag search UI includes a right-side panel to display translations for a single selected language, defaulting to Japanese ('ja'). The main table shows columns for tag, type_name, usage_count, alias, and deprecated. When the result format is 'All', the usage_count is displayed as an em dash ('—'), as defined in the `DataFrameTableModel`.
