
Refactor plan gap: MergedTagReader delegates to TagRepository read methods; if TagRepository drops get_*/list_*/search_*, MergedTagReader breaks unless a new read-only repo class (or TagReader) is introduced and used for base/user repos. Also update all get_default_repository call sites (cli.py, app_services TagSearchService/TagStatisticsService/TagCoreService, tag_search.py, tag_statistics.py) to use get_default_reader, and ensure TagRepository write instances receive an injected reader (get_default_repository should wire it) because create_tag/ensure_tag_with_id rely on reader.
