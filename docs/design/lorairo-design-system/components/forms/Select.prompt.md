A dropdown select with a custom caret. Pass `options` (strings or `{value,label}`) or your own `<option>` children.

```jsx
<Select label="解像度" options={["すべて", "1024px 以上"]} />
<Select label="レーティング" options={["すべて", "PG", "R"]} />
```
