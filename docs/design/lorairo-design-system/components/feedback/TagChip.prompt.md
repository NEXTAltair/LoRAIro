An accent-tinted tag pill for annotation tags. Pass `onRemove` to show a × affordance.

```jsx
<TagChip>1girl</TagChip>
<TagChip>outdoor</TagChip>
<TagChip onRemove={() => drop("solo")}>solo</TagChip>
```
