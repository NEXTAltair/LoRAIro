A neutral, monospaced meta badge for provider names, job kinds and model types — quieter than a status `Chip`.

```jsx
<TypeBadge>anthropic</TypeBadge>
<TypeBadge>multimodal</TypeBadge>
<TypeBadge>model_install</TypeBadge>
```
